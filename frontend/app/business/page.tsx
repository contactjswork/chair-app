'use client';

// /business = le tableau de bord gérant, servi comme accueil du binaire
// CHAIR BUSINESS. Ré-export volontaire (pas de duplication) : mêmes données,
// mêmes liens — seule la porte d'entrée change. Voir layout.tsx du dossier.
export { default } from '@/app/pro/salon-owner/page';
