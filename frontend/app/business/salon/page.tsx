'use client';

// Fiche salon sous la coquille CHAIR BUSINESS — même écran que /pro/salon,
// servi dans l'app gérant (voir app/business/layout.tsx pour la stratégie).
export { default } from '@/app/pro/salon/page';
