'use client';

// Notifications sous la coquille CHAIR BUSINESS — même écran que côté PRO
// (le gérant y reçoit le Pulse du lundi, le radar avis négatif, les
// invitations acceptées…), atteint par la cloche de BusinessShell.
export { default } from '@/app/pro/notifications/page';
